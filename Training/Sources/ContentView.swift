import SwiftUI

/// Placeholder root view.
/// This will be replaced once UI stories are implemented.
struct ContentView: View {
    var body: some View {
        Text("Thunder")
            .font(.largeTitle)
    }
}
